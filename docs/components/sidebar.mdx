---
title: "Sidebar"
date: 2021-12-24
---

# Sidebar Component Documentation

The **Sidebar** component is a flexible and customizable navigation panel. It supports collapsible menus, nested links, and icons, making it ideal for documentation sites, dashboards, and applications with structured navigation.


<Tabs defaultValue="preview" size="sm" className="w-full">
  <TabsList className="grid w-[30%] grid-cols-2">
    <Tab value="preview">Preview</Tab>
    <Tab value="code">Code</Tab>
  </TabsList>
  <TabsContent value="preview">
    <Preview >
      <SidebarProvider defaultOpen={false} defaultSide="left" defaultMaxWidth={300} showIconsOnCollapse={true}>
        <Sidebar className="!h-auto" >
          <SidebarHeader>
            <SidebarTrigger />
            <Title>Documentation</Title>
            <BookOpen className="h-5 w-5" />
          </SidebarHeader>
          <SidebarContent>
            <SidebarMenu>
              <SidebarMenuItem icon={<BookOpen className="h-5 w-5" />} label="Getting Started"/>
              <SidebarMenuItem icon={<Settings className="h-5 w-5" />} label="Configuration"/>
            </SidebarMenu>
          </SidebarContent>
          <SidebarFooter>
            <div className="text-sm text-gray-500">v1.0.0</div>
          </SidebarFooter>
        </Sidebar>
      </SidebarProvider>
    </Preview>
  </TabsContent>
  <TabsContent value="code">
    ```tsx
    import { SidebarProvider, Sidebar, SidebarHeader, SidebarTrigger, Title, SidebarContent, SidebarMenu, SidebarMenuItem, SidebarFooter, NestedLink } from "@/components/sidebar";
    import { BookOpen, Settings, FileText } from "lucide-react";
    
    export default function Example() {
      return (
        <SidebarProvider defaultOpen={false} defaultSide="right" defaultMaxWidth={300} showIconsOnCollapse={true}>
          <Sidebar>
            <SidebarHeader>
              <SidebarTrigger />
              <Title>Documentation</Title>
              <BookOpen className="h-5 w-5" />
            </SidebarHeader>
            <SidebarContent>
              <SidebarMenu>
                <SidebarMenuItem icon={<BookOpen className="h-5 w-5" />} label="Getting Started" href="/docs/getting-started" />
                <SidebarMenuItem icon={<Settings className="h-5 w-5" />} label="Configuration" href="/docs/configuration" />
              </SidebarMenu>
            </SidebarContent>
            <SidebarFooter>
              <div className="text-sm text-gray-500">v1.0.0</div>
            </SidebarFooter>
          </Sidebar>
        </SidebarProvider>
      );
    }
    ```
  </TabsContent>
</Tabs>

# Props

| Prop                 | Type                                      | Default      | Description                                      |
|----------------------|-----------------------------------------|--------------|--------------------------------------------------|
| `defaultOpen`       | `boolean`                                | `false`      | Whether the sidebar is open by default.         |
| `defaultSide`       | `"left" \| "right"`                     | `"right"`    | Defines the sidebar's default position.         |
| `defaultMaxWidth`   | `number`                                 | `300`        | The default maximum width of the sidebar.       |
| `showIconsOnCollapse` | `boolean`                              | `true`       | Whether icons should be visible when collapsed. |
| `...props`          | `React.HTMLAttributes<HTMLDivElement>`   | –            | Supports all standard `div` attributes.         |


